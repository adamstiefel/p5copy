# built-in function converts string to an integer
# mayb add to if then above if i only need access to
# the one that the user wants
value = int(file_contents1)


# this could be used in the original if statement instead of what
# Currently:
# file_contents1 = infile1.read()
# lets you read in the file as an integer
file_contents1 = int(infile1.read())
